all: distance

clean:
	rm -f distance_wrap.py distance.o distance_wrap.cxx distance_wrap.o _distance_wrap.so distance_wrap.pyc

distance: distance.hpp
	swig -python -c++ distance.i
	g++ -fPIC -shared -I/usr/include/python2.7  -o _distance_wrap.so distance_wrap.cxx

#!/usr/bin/env python
#coding:utf-8
import threading
from thrift.transport import TSocket, TTransport
from thrift.protocol import TBinaryProtocol
from pyutil.thrift.transport import TSocketPool
from pyutil.program.icrawler_util import str_dict
from ss.article.common_extractor import Extractor
from ss.article.common_extractor.ttypes import Req, Rsp

class ThriftCommonExtractor(threading.local):

    def __init__(self, host, port):
        transport = TSocketPool.TSocketPool(host, port)
        transport = TTransport.TBufferedTransport(transport)
        protocol = TBinaryProtocol.TBinaryProtocol(transport)
        self.client = Extractor.Client(protocol)
        self.client.transport = transport

    def extract(self, body, info):
        req = Req(body, info)
        str_dict(req)
        try:
            self.client.transport.open()
            rsp = self.client.extract(req)
            return rsp.status, rsp.article
        finally:
            self.client.transport.close()


#!/usr/bin/env python
#coding:utf-8
import threading
from thrift.transport import TSocket, TTransport
from thrift.protocol import TBinaryProtocol
from pyutil.thrift.transport import SocketPool
from pyutil.program.icrawler_util import str_dict
from ss.article.sorter import Sort
from ss.article.sorter.ttypes import Req, Rsp

class ThriftSorter(threading.local):

    def __init__(self, host, port, timeout=2, conn_timeout=0.2):
        transport = SocketPool.TSocketPool(host, port, timeout, conn_timeout)
        transport = TTransport.TBufferedTransport(transport)
        protocol = TBinaryProtocol.TBinaryProtocol(transport)
        self.client = Sort.Client(protocol)
        self.client.transport = transport

    def sort(self, req):
        try:
            self.client.transport.open()
            rsp = self.client.sort(req)
            return rsp
        finally:
            self.client.transport.close()

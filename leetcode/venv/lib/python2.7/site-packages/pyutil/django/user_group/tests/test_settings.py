import os

BASE_DIR = os.path.dirname(__file__)

INSTALLED_APPS = (
    'django.contrib.contenttypes',
    'django.contrib.auth',
    'user_group',
    'user_group.tests',
)

DATABASE_ENGINE = 'sqlite3'

AUTHENTICATION_BACKENDS = (
    'django.contrib.auth.backends.ModelBackend',
    'pyutil.django.user_group.backends.UserGroupBackend',
)

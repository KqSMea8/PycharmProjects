#coding=utf8
from pyutil.program.enum import IntEnum


class AdminFunctionStatus(IntEnum):
    enabled = 1
    disabled = 0

    choices = (
        (enabled, u'启用'),
        (disabled, u'禁用'),
    )

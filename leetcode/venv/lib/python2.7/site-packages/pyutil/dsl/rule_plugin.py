# coding=utf8
import re
from pyutil.net.url import get_domain, get_subdomain, get_main_domain
from pyutil.text.util import text_width, cn_text_width

def remove_tags(html):
    return re.sub(ur'<.+?>', '', html or u'').strip()

TRANS_FUNCS = dict(
        len=lambda x: len(x) if isinstance(x, (basestring, tuple, list, dict)) else 0,
        text_width=text_width,
        cn_text_width=cn_text_width,
        remove_tags=remove_tags,
        domain=get_domain,
        subdomain=get_subdomain,
        main_domain=get_main_domain,
        )

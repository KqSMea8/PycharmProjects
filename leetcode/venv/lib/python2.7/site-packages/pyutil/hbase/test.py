#!/usr/bin/python
from hbase_client import HbaseClient

if __name__ == '__main__':
    client = HbaseClient([], lb_service_name='hbase-recommend-thrift-nb')
    rows = client.scan_rows_results(table_name='debug_message', start_row='1', stop_row='2')
    for row in rows:
        print row

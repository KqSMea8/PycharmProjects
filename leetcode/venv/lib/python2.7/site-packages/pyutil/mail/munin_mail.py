#!/usr/bin/python
# This is a simple script to send an email

from pyutil.mail import send_mail
import sys, getopt

optlist, args = getopt.getopt(sys.argv[1:], 's:')
subject = optlist[0][1]
to = args[0].split(',')
content = sys.stdin.read()
send_mail(subject, content, to, from_name="Munin-Alert")

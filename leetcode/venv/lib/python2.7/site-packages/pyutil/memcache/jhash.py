#! -*- coding: utf-8 -*-

from numpypy import uint32

def oneatatimehash(data):
    hash = uint32(0);
    for c in data:
        hash = uint32(hash + uint32(ord(c)))
        hash = uint32(hash + uint32(hash << 10))
        hash = uint32(hash ^ uint32(hash >> 6))
    hash = uint32(hash + uint32(hash << 3))
    hash = uint32(hash ^ uint32(hash >> 11))
    hash = uint32(hash + uint32(hash << 15))
    return hash;

if __name__ == '__main__':
    print oneatatimehash("Hello World!")
    print oneatatimehash("i love joe!")

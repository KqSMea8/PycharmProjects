#!/usr/bin/python
from pyutil.presto import *
import fileinput

if __name__ == '__main__':
    for sql in fileinput.input():
        with connect('presto', rewrite_queries=True) as client:
            with client.cursor() as cursor:
                cursor.execute(sql)
                print cursor.fetchall()


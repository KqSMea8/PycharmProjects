import os, sys, logging, socket

log_level = logging.INFO
console_log_level = logging.INFO
log_file = 'test.log'

if socket.gethostname() in ('bird', ):
    log_level = logging.DEBUG
    console_log_level = logging.INFO
    log_file = os.path.join(os.path.dirname(__file__), 'test.log')
    host = 'localhost'
try:
    execfile('local_conf.py')
except IOError:
    pass
